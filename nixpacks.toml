# Railway / Nixpacks - build backend from monorepo root
[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["cd backend && npm ci"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && node src/server.js"
